<%= form_with(model: priority_issue, local: true) do |form| %>
  <% if priority_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(priority_issue.errors.count, "error") %> prohibited this priority_issue from being saved:</h2>

      <ul>
        <% priority_issue.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
